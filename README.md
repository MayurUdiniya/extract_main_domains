# extract_main_domains
Script extract main TLD from a list of subdomain. It also take care of edge cases like test.example.com.au 

A subdomain file (i.e domains.txt) file should be present in same directory.
